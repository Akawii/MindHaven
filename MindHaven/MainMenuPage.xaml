<FlyoutPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="MindHaven.MainMenuPage"
            Title="Main Menu">

    <!-- Hamburger Menu -->
    <FlyoutPage.Flyout>
        <ContentPage Title="Menu" BackgroundColor="{StaticResource BackgroundDark}">
            <VerticalStackLayout Padding="20" Spacing="15">

                <Label Text="Menu" FontSize="24" FontAttributes="Bold"
                       TextColor="{StaticResource TextColorLight}" HorizontalOptions="Center"/>

                <Button Text="Emotional Diary"
                        Clicked="OnEmotionalDiaryClicked"
                        BackgroundColor="{StaticResource PrimaryColor}"
                        TextColor="White"/>

                <Button Text="Reports"
                        Clicked="OnReportsPageClicked"
                        BackgroundColor="{StaticResource PrimaryColor}"
                        TextColor="White"/>

            </VerticalStackLayout>
        </ContentPage>
    </FlyoutPage.Flyout>

    <!-- Main Content Area -->
    <FlyoutPage.Detail>
        <NavigationPage>
            <x:Arguments>
                <ContentPage Title="Main Menu" BackgroundColor="{StaticResource BackgroundDark}">

                    <VerticalStackLayout Padding="30" Spacing="15" VerticalOptions="Center">

                        <!-- Logo -->
                        <Image Source="logo.png" HeightRequest="120" Aspect="AspectFit"
                               HorizontalOptions="Center" Margin="0,0,0,20"/>

                        <!-- Welcome Message -->
                        <Label Text="Welcome to MindHaven!"
                               FontSize="28" FontAttributes="Bold"
                               HorizontalOptions="Center"
                               TextColor="{StaticResource TextColorLight}"/>

                        <Label Text="Use the menu to navigate to different features."
                               FontSize="16" HorizontalOptions="Center"
                               TextColor="{StaticResource TextColorLight}"/>
                    </VerticalStackLayout>
                </ContentPage>
            </x:Arguments>
        </NavigationPage>
    </FlyoutPage.Detail>

</FlyoutPage>
